<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/button_gold">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- שם המחבר -->
        <TextView
            android:id="@+id/tvPostAuthor_full"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Author Name"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/tvPostTime_full"
            app:layout_constraintHorizontal_bias="0" />

        <!-- תאריך פרסום -->
        <TextView
            android:id="@+id/tvPostTime_full"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="12:34 PM, 07/05/2025"
            android:textColor="@android:color/darker_gray"
            android:textSize="12sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBaseline_toBaselineOf="@+id/tvPostAuthor_full" />

        <!-- תוכן הפוסט (עד 3 שורות, חיתוך בסוף) -->
        <TextView
            android:id="@+id/textViewPost_full"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:maxLines="3"
            android:text="The content of the post goes here. It can span multiple lines, but will be ellipsized if too long."
            android:textColor="@android:color/black"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@id/tvPostAuthor_full"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- קו מפריד אופקי -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_marginTop="8dp"
            android:background="#DDDDDD"
            app:layout_constraintTop_toBottomOf="@id/textViewPost_full"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- קבוצה של כפתורים (לייק, תגובה, עריכה, מחיקה) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            app:layout_constraintTop_toBottomOf="@id/textViewPost_full"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:gravity="end">

            <!-- כפתור לייק -->
            <ImageButton
                android:id="@+id/buttonLike_full"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_heart"
                android:contentDescription="Like"
                app:tint="@android:color/black" />

            <!-- רווח קטן בין האייקונים -->
            <View
                android:layout_width="8dp"
                android:layout_height="match_parent" />

            <!-- כפתור תגובה -->
            <ImageButton
                android:id="@+id/buttonComment_full"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_comment"
                android:contentDescription="Comment"
                app:tint="@android:color/black" />

            <!-- רווח קטן בין האייקונים -->
            <View
                android:layout_width="8dp"
                android:layout_height="match_parent" />

            <!-- כפתור עריכה (עט) – יוצג רק אם המשתמש הוא בעל הפוסט -->
            <ImageButton
                android:id="@+id/buttonEdit_full"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_edit"
                android:contentDescription="Edit"
                app:tint="@android:color/black" />

            <!-- רווח קטן בין האייקונים -->
            <View
                android:layout_width="8dp"
                android:layout_height="match_parent" />

            <!-- כפתור מחיקה – יוצג רק אם המשתמש הוא בעל הפוסט -->
            <ImageButton
                android:id="@+id/buttonDelete_full"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_red_trash"
                android:contentDescription="Delete"
                app:tint="@android:color/black" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
